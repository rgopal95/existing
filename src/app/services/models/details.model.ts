export interface IDetails {
    id: number,
    name: string, 
    place: string, 
    sports: string[], 
    isInCurrentSem: boolean
}

export interface employee{
    id: number;
    name: string;
    department: string;
    salary: number;
}